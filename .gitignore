# Local publish secrets: stays on this PC, never committed.
publish.env.cmd

# Local build artifacts
pipeline-local.exe
pipeline

# Windows-built local binary (from testing inkbox_pull on the PC)
pipeline.exe
*.exe

pipeline.exe
